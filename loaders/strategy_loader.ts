import {AppLoader} from "../core/app_loader";

export class StrategyLoader extends AppLoader {

    protected _repository = 'strategy'

    protected _onCall(target: any) {
    }

    protected _onGenerate(repository: string) {
    }

}
