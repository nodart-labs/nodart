const { OrmSeedSource } = require("nodart")

class [NAME]Seed extends OrmSeedSource {

    seeds = {
SEED>>>
        [SEED_NAME]: (client) => {
            return client('[TABLE]').truncate()
                .then(function () {
                    return client('[TABLE]').insert([
                        {},
                    ])
                })
        },
SEED>>>
    }
}

module.exports = {[NAME]Seed}
