import { OrmSeedSource, nodart } from "nodart";

export class [NAME]Seed extends OrmSeedSource {

    readonly seeds = <nodart.orm.OrmSeedInterface>{
SEED>>>
        [SEED_NAME]: (client) => {
            return client('[TABLE]').truncate()
                .then(function () {
                    return client('[TABLE]').insert([
                        {},
                    ])
                })
        },
SEED>>>
    }
}
